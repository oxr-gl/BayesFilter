# Streaming Manual VJP Phase 6 Subplan: Local Parity Ladder

status: READY_FOR_CLAUDE_REVIEW
date: 2026-06-23
phase: S6-LOCAL-PARITY-LADDER

## Phase Objective

Run the CPU-hidden local parity ladder for the newly wired opt-in streaming
blockwise manual VJP route before any GPU or large-`N` experiment.  S6 must
exercise both the transport-core route and a tiny LEDH-PFPF-OT value-and-score
path that selects the new route explicitly.

## Entry Conditions

- S5 result status is `PASSED`.
- S5 bounded Claude result review R2 returned `VERDICT: AGREE`.
- The new route remains opt-in:
  `manual_streaming_blockwise_vjp_finite_sinkhorn_stopped_scale_keys`.
- The old replay route remains:
  `manual_streaming_finite_sinkhorn_stopped_scale_keys`.
- Default dispatch remains `transport_gradient_mode="filterflow_clipped"` and
  `transport_plan_mode="dense"` unless a caller explicitly selects another
  route.
- GPU, P82 FD, and `N=10000` commands are still forbidden in S6.

## Required Artifacts

- S6 result:
  `docs/plans/bayesfilter-ledh-pfpf-ot-streaming-manual-vjp-phase6-local-parity-ladder-result-2026-06-23.md`
- S6 blocker result, if needed, reuses the S6 result path above with
  `status: BLOCKED`; do not create an untracked alternate blocker path.
- Refreshed S7 subplan:
  `docs/plans/bayesfilter-ledh-pfpf-ot-streaming-manual-vjp-phase7-gpu-memory-ladder-subplan-2026-06-23.md`
- Updated ledgers:
  `docs/plans/bayesfilter-ledh-pfpf-ot-streaming-manual-vjp-visible-execution-ledger-2026-06-23.md`
  and
  `docs/plans/bayesfilter-ledh-pfpf-ot-streaming-manual-vjp-claude-review-ledger-2026-06-23.md`
- Updated visible stop handoff:
  `docs/plans/bayesfilter-ledh-pfpf-ot-streaming-manual-vjp-visible-stop-handoff-2026-06-23.md`
- Optional diagnostic JSON only if generated by the existing test harness; do
  not invent a new long-running benchmark artifact in S6.

## Required Checks/Tests/Reviews

Before execution, run a skeptical plan audit.  The audit must confirm:

- the comparator is tiny dense/manual and tiny autodiff diagnostics only;
- no Zhao-Cui comparator is used;
- no P82 FD comparison is launched;
- no GPU command is launched;
- no `N=10000` command is launched;
- no `transport_ad_mode="full"` governed route is reintroduced;
- the commands answer S6 local parity and boundary questions rather than S7
  memory feasibility.

Implementation work is limited to adding a focused local test if needed:

- Add one opt-in value-and-score smoke for
  `MANUAL_STREAMING_BLOCKWISE_VJP_FINITE_TRANSPORT_GRADIENT_MODE` in
  `tests/test_experimental_batched_ledh_pfpf_ot_tf.py`.
- The smoke must mirror the existing tiny manual-streaming value-and-score
  shape/finite/eager-vs-graph test, but use `transport_plan_mode="streaming"`,
  scalar epsilon, stabilized transport AD mode, small `sinkhorn_iterations`, and
  row/column chunks that include exact local chunks for `N=4`.
- The smoke must not use FD as a promotion criterion.  `GradientTape` inside
  `batched_ledh_pfpf_ot_value_and_score_tf` is allowed as a high-level local
  diagnostic; `GradientTape` inside the new blockwise route backward is not
  allowed.

Run these exact local checks:

```text
CUDA_VISIBLE_DEVICES=-1 python -m pytest tests/test_ledh_pfpf_ot_manual_adjoint_primitives.py -k "streaming_softmin or streaming_transport_from_potentials or streaming_sinkhorn_recursion or blockwise_route" -q
```

```text
CUDA_VISIBLE_DEVICES=-1 python -m pytest tests/test_experimental_batched_ledh_pfpf_ot_tf.py -k "m6_manual_streaming_value_and_score_tiny_opt_in_smoke or s6_blockwise_manual_streaming_value_and_score_tiny_opt_in_smoke" -q
```

```text
CUDA_VISIBLE_DEVICES=-1 python -m py_compile experiments/dpf_implementation/tf_tfp/resampling/annealed_transport_tf.py experiments/dpf_implementation/tf_tfp/filters/experimental_batched_ledh_pfpf_ot_tf.py tests/test_ledh_pfpf_ot_manual_adjoint_primitives.py tests/test_experimental_batched_ledh_pfpf_ot_tf.py
```

```text
rg -n "manual_streaming_blockwise_vjp_finite_sinkhorn_stopped_scale_keys|GradientTape|\[B,N,N\]" experiments/dpf_implementation/tf_tfp/resampling/annealed_transport_tf.py experiments/dpf_implementation/tf_tfp/filters/experimental_batched_ledh_pfpf_ot_tf.py tests/test_ledh_pfpf_ot_manual_adjoint_primitives.py tests/test_experimental_batched_ledh_pfpf_ot_tf.py
```

The source-scan classification in the S6 result must explicitly classify every
production-code `GradientTape` hit and must state whether any hit is inside
`_filterflow_manual_streaming_blockwise_vjp_finite_transport_stopped_scale_keys`
or its nested custom-gradient `grad`.

The hidden-dense-streaming and unsupported-mode vetoes are operationalized by
`test_blockwise_route_matches_dense_and_preserves_streaming_metadata` and
`test_blockwise_route_rejects_unsupported_combinations_and_preserves_defaults`
from the first pytest command.  The S6 result must quote their pass status and
must state that the streaming transport matrix size remained zero and that
unsupported dense plan, warmstart, `transport_ad_mode="full"`, and vector
epsilon cases rejected for the new route.

```text
git diff --check -- experiments/dpf_implementation/tf_tfp/resampling/annealed_transport_tf.py experiments/dpf_implementation/tf_tfp/filters/experimental_batched_ledh_pfpf_ot_tf.py tests/test_ledh_pfpf_ot_manual_adjoint_primitives.py tests/test_experimental_batched_ledh_pfpf_ot_tf.py docs/plans/bayesfilter-ledh-pfpf-ot-streaming-manual-vjp-phase6-local-parity-ladder-subplan-2026-06-23.md
```

After local checks, write the S6 result and request bounded exact-path Claude
review of that result.

## Evidence Contract

| Field | Contract |
|---|---|
| Question | Does the new streaming blockwise manual VJP route pass local primitive, route, and tiny value-and-score parity/boundary checks before GPU work? |
| Baseline/comparator | Dense manual tiny fixtures, old streaming replay route for value parity only, and tiny TensorFlow autodiff diagnostics; Zhao-Cui is not a comparator. |
| Primary pass criterion | All listed CPU-hidden local tests pass; new route value/gradient parity stays within existing local tolerances; tiny value-and-score smoke is finite and graph/eager consistent; source-scan classification confirms no `GradientTape` in the new blockwise route backward. |
| Veto diagnostics | Any local nonfinite value/gradient; blockwise route value/gradient parity failure; tiny value-and-score smoke failure; route mismatch; source scan finds `GradientTape` in the new blockwise route backward; hidden dense streaming transport matrix; unsupported mode accepted; GPU/P82/`N=10000` command launched. |
| Explanatory only | Runtime, tolerance maxima, graph/eager deltas, high-level value-and-score `GradientTape`, and source-scan locations outside the new route backward. |
| Not concluded | No GPU memory success, no `N=10000` feasibility, no P82 FD agreement, no HMC/default readiness, no production readiness, no scientific superiority. |
| Artifact | S6 result plus updated ledgers and stop handoff. |

## Forbidden Claims/Actions

- Do not run GPU jobs until S7.
- Do not launch P82 FD.
- Do not run `N=10000`.
- Do not use Zhao-Cui as comparator.
- Do not reintroduce `transport_ad_mode="full"` as a governed route.
- Do not change default route, default policy, public API exposure, HMC policy,
  model/funding boundaries, or scientific criteria.
- Do not claim large-`N` memory feasibility, FD consistency, posterior
  correctness, HMC/default readiness, or production readiness from S6.

## Exact Next-Phase Handoff Conditions

Advance to S7 only if:

- S6 result passes all listed local checks;
- S6 result passes bounded Claude exact-path review;
- S7 subplan is refreshed to include trusted/elevated GPU preflight,
  route-metadata checks, memory ladder rungs, artifact paths, stop conditions,
  and explicit prohibition on S8/P82 advancement if S7 fails;
- the visible stop handoff is updated to
  `S6_PASSED_READY_FOR_S7_SUBPLAN_REVIEW`.

## Stop Conditions

Stop and write an S6 blocker result if:

- any required local test/check fails and cannot be repaired within S6 scope;
- the source scan finds `GradientTape` in the new blockwise route backward;
- the value-and-score smoke requires changing scientific criteria, default
  policy, or runtime/GPU boundaries;
- S6 would need GPU/P82/`N=10000` evidence to pass;
- the S6 result or S7 subplan fails to converge under bounded Claude review
  within five rounds for the same blocker.

## Skeptical Plan Audit

Audit result before execution: pass after this refresh.  The old S6 draft was
too loose because it named a local parity ladder without exact selectors or a
concrete high-level value-and-score route smoke.  This refreshed plan repairs
that by pinning selectors, artifact paths, forbidden actions, and the exact
scope of the allowed value-and-score diagnostic.  The commands answer S6 local
parity and boundary questions only; they do not attempt S7 memory feasibility
or P82 FD consistency.

## End-Of-Phase Protocol

1. Run required local checks.
2. Write the S6 result or blocker.
3. Draft or refresh S7.
4. Review S7 for consistency, correctness, feasibility, artifact coverage, and
   boundary safety.
5. Request bounded exact-path Claude review for material S6 result and S7
   subplan artifacts, repairing visibly and looping at most five rounds for the
   same blocker.
