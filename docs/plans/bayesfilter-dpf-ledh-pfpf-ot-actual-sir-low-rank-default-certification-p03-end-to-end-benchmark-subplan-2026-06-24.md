# P03 End-To-End Actual-SIR Benchmark Harness/Gate Subplan

Date: 2026-06-24

Status: `COMPLETE_P03_PASSED`

## Phase Objective

Run a trusted-GPU end-to-end actual-SIR d18 benchmark gate for the locked
low-rank candidate `r16_eps0p25_alpha1em08_it120` against the current streaming
GPU/TF32 comparator, preserving paired seeds, actual-SIR semantics,
compiled-core timing, hard-veto diagnostics, whole-row wall time, and artifact
provenance.

P03 answers whether the low-rank candidate remains runtime-viable under a fresh
approved paired benchmark gate and records descriptive end-to-end performance
evidence for the next decision. One fresh seed batch cannot by itself establish
performance superiority or default readiness. It does not change defaults,
public API, package metadata, algorithmic code, or HMC behavior.

## Entry Conditions Inherited From Previous Phase

- P00 governance result passes.
- P01 evidence/default-surface audit result passes.
- P02 implementation-path/no-NumPy audit result passes.
- Candidate lock is `r16_eps0p25_alpha1em08_it120`.
- Active implementation path is the TensorFlow low-rank solver route, not the
  older fixture route.
- Existing evidence anchor remains the N3072 replicated closeout from
  2026-06-23.
- Explicit user approval is required before any trusted GPU benchmark command
  in P03.
- No approval is inherited for default-code changes, public API changes,
  package installs, network fetches, destructive git operations, HMC readiness
  claims, or scientific claims.

## Required Artifacts

- This subplan.
- P03 result:
  `docs/plans/bayesfilter-dpf-ledh-pfpf-ot-actual-sir-low-rank-default-certification-p03-end-to-end-benchmark-result-2026-06-24.md`
- Benchmark aggregate JSON:
  `docs/benchmarks/actual-sir-low-rank-default-certification-p03-end-to-end-2026-06-24.json`
- Benchmark aggregate Markdown:
  `docs/benchmarks/actual-sir-low-rank-default-certification-p03-end-to-end-2026-06-24.md`
- Child row JSON/Markdown/log artifacts generated by the tuning grid harness.
- Updated execution ledger.
- Updated Claude review ledger if Claude is used.
- P04 draft subplan:
  `docs/plans/bayesfilter-dpf-ledh-pfpf-ot-actual-sir-low-rank-default-certification-p04-n4096-resource-boundary-subplan-2026-06-24.md`

## Required Checks, Tests, And Reviews

- Skeptical plan audit before runtime execution.
- Trusted GPU precheck before benchmark execution:
  `nvidia-smi`
- GPU selection rule:
  choose a CUDA ordinal only after the trusted precheck. Prefer physical GPU 1
  if present and not materially busier than alternatives because the existing
  N3072 evidence used visible CUDA `1`; otherwise select an available GPU and
  record the selected physical index, UUID, memory state, and reason in the P03
  result before running the benchmark. The command below is the GPU-1 form and
  must be updated visibly if a different ordinal is selected.
- Dry-run/path-length gate:
  run the tuning-grid harness in `--mode dry-run` with the exact P03 benchmark
  arguments and verify the aggregate/row filenames do not exceed component
  limits.
- Benchmark command, after approval and after selecting the CUDA ordinal:
  `python docs/benchmarks/run_actual_sir_low_rank_tuning_grid.py --mode execute --route both --candidate-ids r16_eps0p25_alpha1em08_it120 --batch-seeds 81141,81142 --time-steps 20 --num-particles 3072 --transport-policy active-all --low-rank-ranks 16 --low-rank-assignment-epsilons 0.25 --low-rank-alphas 1e-8 --low-rank-max-projection-iterations-list 120 --low-rank-convergence-threshold 1e-6 --low-rank-denominator-floor 1e-30 --streaming-timing-source compiled_core --low-rank-timing-source compiled_core --sinkhorn-iterations 10 --sinkhorn-epsilon 1.0 --annealed-scaling 0.9 --annealed-convergence-threshold 0.001 --row-chunk-size 128 --col-chunk-size 128 --particle-chunk-size 64 --warmups 1 --repeats 2 --dtype float32 --tf32-mode enabled --device-scope visible --device /GPU:0 --expect-device-kind gpu --cuda-visible-devices 1 --jit-compile --phase-id-prefix ACTUAL-SIR-LR-DEFAULT-P03 --row-timeout-seconds 1200 --output docs/benchmarks/actual-sir-low-rank-default-certification-p03-end-to-end-2026-06-24.json --markdown-output docs/benchmarks/actual-sir-low-rank-default-certification-p03-end-to-end-2026-06-24.md --quiet`
- Artifact validator over the aggregate JSON and row artifacts:
  - exact candidate id;
  - exact seeds `81141,81142`;
  - exact shape `B=2`, `T=20`, `N=3072`, `D=18`, `M=9`;
  - route request `both`;
  - status `PASS`;
  - hard vetoes `[]`;
  - actual-SIR semantics pass;
  - paired comparability pass;
  - low-rank provenance complete;
  - GPU/TF32 provenance complete;
  - active route invocations equal active resampling mask count;
  - no dense transport materialization;
  - end-to-end row wall time and warm-call summaries present.
- Focused local syntax check:
  `python -m py_compile docs/benchmarks/benchmark_actual_sir_low_rank_route_validation.py docs/benchmarks/run_actual_sir_low_rank_tuning_grid.py tests/test_actual_sir_low_rank_route_validation.py tests/test_actual_sir_low_rank_tuning_grid.py`
- Focused tests:
  `python -m pytest tests/test_actual_sir_low_rank_route_validation.py tests/test_actual_sir_low_rank_tuning_grid.py -q`
- Boundary scan over P03 result and P04 draft.
- Claude read-only review if the dry-run, benchmark result, or P04 subplan
  raises a material consistency, feasibility, artifact-coverage, or boundary
  question.

## Skeptical Plan Audit

| Audit item | Status |
| --- | --- |
| Wrong baseline | Guarded: P03 uses route `both` so the streaming comparator and low-rank candidate share seeds, shape, dtype, TF32, GPU, and timing contract. |
| Proxy metric promoted | Guarded: warm ratio is descriptive only; P03 requires paired comparability, hard-veto pass, whole-row wall time, provenance, and end-to-end artifact validation before any continuation discussion. |
| Missing stop conditions | Guarded by GPU availability, dry-run/path-length, timeout, artifact, hard-veto, claim, and approval stops. |
| Unfair comparison | Guarded: one harness invocation creates paired streaming and low-rank rows for the same seed batch and request signature. |
| Hidden assumptions | Guarded: P03 does not change the candidate after seeing results and does not substitute the fallback candidate. |
| Stale context | Guarded: P03 starts from P02 and reruns syntax/tests before interpreting results. |
| Environment mismatch | Requires trusted `nvidia-smi`, explicit CUDA ordinal selection, visible GPU scope, `--expect-device-kind gpu`, and GPU/TF32 provenance in artifacts. |
| Artifact mismatch | Requires dry-run, aggregate JSON/Markdown, row artifacts, logs, result note, and P04 draft. |

Audit conclusion: P03 is ready for human approval of trusted GPU benchmark
runtime. It must not execute the benchmark command until that approval is
explicitly given.

## Evidence Contract

- Question: under a fresh trusted-GPU actual-SIR d18 N3072 paired benchmark,
  does the locked low-rank candidate remain viable and provide end-to-end
  performance evidence strong enough to continue default-certification?
- Baseline/comparator: current streaming GPU/TF32 actual-SIR route in the same
  `--route both` run.
- Primary pass criterion: aggregate and row artifacts pass all hard vetoes,
  actual-SIR semantics, paired comparability, low-rank provenance, GPU/TF32
  provenance, route-fired/nonmaterialization checks, and preserve whole-row wall
  time and warm-call timing summaries.
- Timing role: warm median streaming-over-low-rank and row wall time are
  descriptive diagnostics in P03. If hard vetoes pass but timing is not
  descriptively favorable, P03 may still pass the validity gate while handing
  off to a repair, replication, or uncertainty subplan. Timing alone cannot
  promote, reject, or default the candidate in P03.
- Veto diagnostics: trusted GPU unavailable; dry-run path-length failure; row
  timeout; nonzero benchmark exit; missing/corrupt artifacts; hard vetoes;
  actual-SIR semantics failure; paired comparability failure; route invocation
  mismatch; dense materialization; missing GPU/TF32 provenance; unsupported
  default/scientific/HMC claim.
- Explanatory diagnostics: first-call time, warm-call timings, whole-row wall
  time, memory snapshots, log-likelihood deltas, filtered mean/variance deltas,
  ESS, residual magnitudes, projection iterations, warnings, and filename
  lengths.
- What will not be concluded: default readiness, statistical superiority,
  posterior correctness, HMC readiness, dense Sinkhorn equivalence, public API
  readiness, N4096 feasibility, formal memory scaling, production readiness,
  or scientific validity.
- Artifact preserving result: P03 result, benchmark aggregate JSON/Markdown,
  row JSON/Markdown/log artifacts, and updated ledgers.

## Forbidden Claims And Actions

- Do not run benchmark runtime before explicit user approval.
- Do not change defaults, public exports, API, package metadata, dependencies,
  model files, or algorithmic code.
- Do not use NumPy as BayesFilter-owned algorithmic implementation.
- Do not substitute `r16_eps0p125_alpha1em08_it120` or any other fallback after
  seeing P03 results.
- Do not treat warm ratio alone as default-readiness evidence.
- Do not claim statistical superiority from one fresh seed batch.
- Do not claim posterior correctness, HMC readiness, dense Sinkhorn equivalence,
  public API readiness, N4096 feasibility, formal memory scaling, production
  readiness, or scientific validity.
- Do not treat Claude as execution authority.

## Exact Next-Phase Handoff Conditions

- If P03 passes, draft or refresh P04 N4096 resource-boundary subplan and
  request any required GPU/runtime approval.
- If P03 fails a hard veto that indicates a harness or artifact bug, write a
  blocker/repair result and ask for explicit approval before code changes.
- If P03 fails paired comparability without invalidating the harness/artifacts,
  write a candidate-validity-failed result and hand off to a reviewed
  repair/selection subplan rather than silently swapping candidates.
- If P03 passes hard vetoes but timing is not descriptively favorable, write a
  validity-pass/timing-repair-needed result and hand off to a reviewed repair,
  replication, or uncertainty subplan rather than rejecting or promoting the
  candidate from one fresh seed batch.
- If trusted GPU is unavailable or dry-run/path-length fails, write a blocker
  result and stop.
- If local tests fail, write a blocker result and stop for focused repair.

## Stop Conditions

- User approval for trusted GPU runtime is absent.
- `nvidia-smi` cannot establish trusted GPU context.
- Selected CUDA ordinal is absent, materially unsuitable, or not recorded in
  the result.
- Dry-run/path-length gate fails.
- Benchmark command exits nonzero or times out.
- Required aggregate/row/log artifacts are missing or corrupt.
- Any row hard veto fires.
- Actual-SIR semantics, paired comparability, low-rank provenance, GPU/TF32
  provenance, or route-fired/nonmaterialization check fails.
- Boundary scan finds unsupported claims.
- Any action would require default-code change, public API change, package or
  network access, destructive git operation, or scientific/HMC claim.

## End-Of-Subplan Duties

1. Run the required local checks and approved trusted-GPU benchmark gate.
2. Write the P03 result or blocker result.
3. Draft or refresh the P04 subplan.
4. Review P04 for consistency, correctness, feasibility, artifact coverage, and
   boundary safety.

## Subplan Self-Review

- Consistency: follows P02 and keeps the locked candidate/comparator contract.
- Correctness: requires paired route `both`, hard-veto screens, actual-SIR
  semantics, and GPU/TF32 provenance.
- Feasibility: one N3072 two-seed candidate row with timeout and dry-run gate.
- Artifact coverage: aggregate JSON/Markdown, row artifacts, logs, result,
  ledgers, and P04 draft.
- Boundary safety: requires explicit approval before GPU runtime and forbids
  default/API/code/HMC/science boundary crossing.
