# P04 N4096 Resource-Boundary Feasibility Subplan

Date: 2026-06-24

Status: `COMPLETE_P04_PASSED`

## Phase Objective

Run a separately approved trusted-GPU actual-SIR d18 N4096 resource-boundary
gate for locked candidate `r16_eps0p25_alpha1em08_it120` against the current
streaming GPU/TF32 comparator, preserving paired seeds, actual-SIR semantics,
compiled-core timing, hard-veto diagnostics, nonmaterialization evidence,
whole-row wall time, and artifact provenance.

P04 answers whether the locked low-rank candidate remains feasible and valid at
N4096 under the same actual-SIR route-validation harness. P04 is a
resource-boundary feasibility gate, not a default switch, statistical ranking,
posterior-correctness proof, HMC-readiness gate, dense Sinkhorn equivalence
claim, or public API change.

## Entry Conditions Inherited From Previous Phase

- P00 governance result passes.
- P01 evidence/default-surface audit result passes.
- P02 implementation-path/no-NumPy audit result passes.
- P03 end-to-end actual-SIR N3072 benchmark result passes its hard-veto gate.
- Candidate lock remains `r16_eps0p25_alpha1em08_it120`.
- Active implementation path remains
  `experiments/dpf_implementation/tf_tfp/resampling/low_rank_coupling_solver_tf.py`
  through `low_rank_coupling_solver_resample_tensors_tf(...)`.
- Existing P03 metadata caveat is known: benchmark harness artifacts carry
  legacy `plan_path` constants. P04 may record the same caveat, but must not
  let stale plan metadata obscure command, phase id, output path, or result
  provenance.
- Explicit user approval is required before trusted GPU benchmark runtime in
  P04. P03 runtime approval does not carry over.
- The P04 benchmark command requires explicit P04 runtime approval.
- No approval is inherited for default-code changes, public API changes,
  package installs, network fetches, destructive git operations, HMC readiness
  claims, or scientific claims.

## Required Artifacts

- This subplan.
- P04 result:
  `docs/plans/bayesfilter-dpf-ledh-pfpf-ot-actual-sir-low-rank-default-certification-p04-n4096-resource-boundary-result-2026-06-24.md`
- Benchmark aggregate JSON:
  `docs/benchmarks/actual-sir-low-rank-default-certification-p04-n4096-resource-boundary-2026-06-24.json`
- Benchmark aggregate Markdown:
  `docs/benchmarks/actual-sir-low-rank-default-certification-p04-n4096-resource-boundary-2026-06-24.md`
- Child row JSON/Markdown/log artifacts generated by the tuning grid harness.
- Updated execution ledger.
- Updated Claude review ledger if Claude is used.
- P05 draft subplan:
  `docs/plans/bayesfilter-dpf-ledh-pfpf-ot-actual-sir-low-rank-default-certification-p05-default-implementation-subplan-2026-06-24.md`

## Required Checks, Tests, And Reviews

- Skeptical plan audit before runtime execution.
- Local syntax check before benchmark runtime:
  `python -m py_compile docs/benchmarks/benchmark_actual_sir_low_rank_route_validation.py docs/benchmarks/run_actual_sir_low_rank_tuning_grid.py tests/test_actual_sir_low_rank_route_validation.py tests/test_actual_sir_low_rank_tuning_grid.py`
- Focused tests before benchmark runtime:
  `python -m pytest tests/test_actual_sir_low_rank_route_validation.py tests/test_actual_sir_low_rank_tuning_grid.py -q`
- Trusted GPU precheck before benchmark execution:
  `nvidia-smi`
- GPU selection rule:
  choose a CUDA ordinal only after the trusted precheck. Prefer physical GPU 1
  if present and not materially busier than alternatives because P03 used
  physical CUDA `1`; otherwise select an available GPU and record the selected
  physical index, UUID, memory state, and reason in the P04 result before
  running the benchmark. The command below is the GPU-1 form and must be
  updated visibly if a different ordinal is selected.
- Dry-run/path-length gate:
  run the tuning-grid harness in `--mode dry-run` with the exact P04 benchmark
  arguments and verify aggregate/row filenames do not exceed component limits.
- Benchmark command, only after P04 review convergence and explicit P04 runtime
  approval:
  `python docs/benchmarks/run_actual_sir_low_rank_tuning_grid.py --mode execute --route both --candidate-ids r16_eps0p25_alpha1em08_it120 --batch-seeds 81143,81144 --time-steps 20 --num-particles 4096 --transport-policy active-all --low-rank-ranks 16 --low-rank-assignment-epsilons 0.25 --low-rank-alphas 1e-8 --low-rank-max-projection-iterations-list 120 --low-rank-convergence-threshold 1e-6 --low-rank-denominator-floor 1e-30 --streaming-timing-source compiled_core --low-rank-timing-source compiled_core --sinkhorn-iterations 10 --sinkhorn-epsilon 1.0 --annealed-scaling 0.9 --annealed-convergence-threshold 0.001 --row-chunk-size 128 --col-chunk-size 128 --particle-chunk-size 64 --warmups 1 --repeats 2 --dtype float32 --tf32-mode enabled --device-scope visible --device /GPU:0 --expect-device-kind gpu --cuda-visible-devices 1 --jit-compile --phase-id-prefix ACTUAL-SIR-LR-DEFAULT-P04 --row-timeout-seconds 1800 --output docs/benchmarks/actual-sir-low-rank-default-certification-p04-n4096-resource-boundary-2026-06-24.json --markdown-output docs/benchmarks/actual-sir-low-rank-default-certification-p04-n4096-resource-boundary-2026-06-24.md --quiet`
- Artifact validator over aggregate JSON and row artifacts:
  - exact candidate id;
  - exact seeds `81143,81144`;
  - exact shape `B=2`, `T=20`, `N=4096`, `D=18`, `M=9`;
  - route request `both`;
  - status `PASS`;
  - hard vetoes `[]`;
  - actual-SIR semantics pass;
  - paired comparability pass under declared thresholds;
  - low-rank provenance complete;
  - GPU/TF32 provenance complete;
  - active route invocations equal active resampling mask count;
  - no dense transport materialization;
  - end-to-end row wall time and warm-call summaries present.
- Boundary scan over P04 result and P05 draft.
- Claude Opus/max read-only review of this P04 subplan before runtime because
  P04 is a material GPU resource-boundary phase.

## Skeptical Plan Audit

| Audit item | Status |
| --- | --- |
| Wrong baseline | Guarded: P04 uses route `both` so the streaming comparator and low-rank candidate share seeds, shape, dtype, TF32, GPU, and timing contract. |
| Proxy metric promoted | Guarded: timing remains descriptive; P04 promotes only feasibility/validity continuation if hard-veto and provenance checks pass. |
| Missing stop conditions | Guarded by approval, GPU availability, dry-run/path-length, timeout, artifact, hard-veto, claim, and local-check stops. |
| Unfair comparison | Guarded: one harness invocation creates paired streaming and low-rank rows for the same seed batch and request signature. |
| Hidden assumptions | Guarded: P04 does not change the locked candidate or silently use the fallback candidate after seeing results. |
| Stale context | Guarded: P04 starts from P03 result and reruns syntax/tests before runtime. Legacy harness `plan_path` constants are recorded as a known metadata caveat, not hidden. |
| Environment mismatch | Requires trusted `nvidia-smi`, explicit CUDA ordinal selection, visible GPU scope, `--expect-device-kind gpu`, and GPU/TF32 provenance. |
| Artifact mismatch | Requires dry-run, aggregate JSON/Markdown, row artifacts, logs, result note, ledgers, and P05 draft. |

Audit conclusion: P04 is draft-ready for local checks and Claude read-only
review. It must not execute benchmark runtime until review converges and the
user explicitly approves the P04 trusted GPU gate.

## Evidence Contract

- Question: under a trusted-GPU actual-SIR d18 N4096 paired benchmark, does the
  locked low-rank candidate remain feasible and valid enough to continue toward
  bounded default-readiness review?
- Baseline/comparator: current streaming GPU/TF32 actual-SIR route in the same
  `--route both` run.
- Primary pass criterion: aggregate and row artifacts pass all hard vetoes,
  actual-SIR semantics, paired comparability, low-rank provenance, GPU/TF32
  provenance, route-fired/nonmaterialization checks, and preserve whole-row wall
  time and warm-call timing summaries.
- Timing role: warm median streaming-over-low-rank and row wall time are
  descriptive diagnostics in P04. If hard vetoes pass but timing is not
  descriptively favorable, P04 may still pass feasibility while handing off to a
  repair, replication, or uncertainty subplan. Timing alone cannot promote,
  reject, or default the candidate in P04.
- Veto diagnostics: P04 approval absent; trusted GPU unavailable; dry-run
  path-length failure; row timeout; nonzero benchmark exit; missing/corrupt
  artifacts; hard vetoes; actual-SIR semantics failure; paired comparability
  failure; route invocation mismatch; dense materialization; missing GPU/TF32
  provenance; unsupported default/scientific/HMC claim.
- Explanatory diagnostics: first-call time, warm-call timings, whole-row wall
  time, memory snapshots, log-likelihood deltas, filtered mean/variance deltas,
  ESS, residual magnitudes, projection iterations, warnings, and filename
  lengths.
- Not concluded: default readiness, statistical superiority,
  posterior correctness, HMC readiness, dense Sinkhorn equivalence, public API
  readiness, formal memory scaling, production readiness, or scientific
  validity.
- Artifact preserving result: P04 result, benchmark aggregate JSON/Markdown,
  row JSON/Markdown/log artifacts, and updated ledgers.

## Forbidden Claims And Actions

- Do not run P04 benchmark runtime before P04 subplan review convergence and
  explicit P04 user approval.
- Do not change defaults, public exports, API, package metadata, dependencies,
  model files, or algorithmic code.
- Do not use NumPy as BayesFilter-owned algorithmic implementation.
- Do not substitute `r16_eps0p125_alpha1em08_it120` or any other fallback after
  seeing P04 results.
- Do not treat warm ratio alone as default-readiness evidence.
- Do not claim statistical superiority from one N4096 seed batch.
- Do not claim posterior correctness, HMC readiness, dense Sinkhorn
  equivalence, public API readiness, formal memory scaling, production
  readiness, or scientific validity.
- Do not treat Claude as execution authority.

## Exact Next-Phase Handoff Conditions

- If P04 passes, write the P04 result, draft or refresh P05 default-route
  implementation subplan, and request required approval before any default-code
  or public API change.
- If P04 fails a hard veto that indicates a harness or artifact bug, write a
  blocker/repair result and ask for explicit approval before code changes.
- If P04 fails paired comparability without invalidating harness/artifacts,
  write a candidate-validity-failed result and hand off to a reviewed
  repair/selection subplan rather than silently swapping candidates.
- If P04 passes hard vetoes but timing is not descriptively favorable, write a
  feasibility-pass/timing-repair-needed result and hand off to a reviewed
  repair, replication, or uncertainty subplan rather than rejecting or promoting
  the candidate from one seed batch.
- If trusted GPU is unavailable, dry-run/path-length fails, or local tests fail,
  write a blocker result and stop.

## Stop Conditions

- P04 subplan review does not converge after five rounds for the same blocker.
- User approval for P04 trusted GPU runtime is absent.
- `nvidia-smi` cannot establish trusted GPU context.
- Selected CUDA ordinal is absent, materially unsuitable, or not recorded in
  the result.
- Dry-run/path-length gate fails.
- Benchmark command exits nonzero or times out.
- Required aggregate/row/log artifacts are missing or corrupt.
- Any row hard veto fires.
- Actual-SIR semantics, paired comparability, low-rank provenance, GPU/TF32
  provenance, or route-fired/nonmaterialization check fails.
- Boundary scan finds unsupported claims.
- Any action would require default-code change, public API change, package or
  network access, destructive git operation, or scientific/HMC claim.

## End-Of-Subplan Duties

1. Run required local checks and, after approval, the trusted-GPU benchmark
   gate.
2. Write the P04 result or blocker result.
3. Draft or refresh the P05 subplan.
4. Review P05 for consistency, correctness, feasibility, artifact coverage, and
   boundary safety.

## Subplan Self-Review

- Consistency: follows P03 and keeps the locked candidate/comparator contract.
- Correctness: requires paired route `both`, hard-veto screens, actual-SIR
  semantics, and GPU/TF32 provenance.
- Feasibility: one N4096 two-seed candidate row with timeout and dry-run gate;
  timeout is higher than P03 because N4096 increases the streaming comparator
  workload.
- Artifact coverage: aggregate JSON/Markdown, row artifacts, logs, result,
  ledgers, and P05 draft.
- Boundary safety: requires review convergence and explicit approval before
  P04 runtime and forbids default/API/code/HMC/science boundary crossing.
