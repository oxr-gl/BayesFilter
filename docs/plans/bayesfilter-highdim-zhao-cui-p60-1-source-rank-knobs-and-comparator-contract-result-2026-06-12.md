# P60-1 Result: Source Rank Knobs And Comparator Contract

metadata_date: 2026-06-12
status: PASS_P60_1_SOURCE_RANK_COMPARATOR_CONTRACT

## Evidence Contract

| Field | Result |
| --- | --- |
| Question | Which Zhao-Cui source-code knobs define the author-SIR rank route, and what comparator contract is needed before BayesFilter may claim same-route rank convergence? |
| Baseline/comparator | Zhao-Cui `eg3_sir/mainscript.m`, `models/full_sol.m`, `models/computeL.m`, `models/ESS.m`, and `deep-tensor.dev/src/Options/TTOption.m`; P59-9e execution-only artifact. |
| Primary criterion | Source-cited contract artifact lists author-code facts, fixed-variant choices, comparator configurations, declared tolerances, veto diagnostics, and artifact paths before P60-2 runs. |
| Veto diagnostics | UKF/memory/finite values as correctness evidence; 18D reapproximation target; nonempty theta block for author row; old all-grid/local route; post-hoc tolerance selection. |
| Not concluded | No rank convergence, no correctness candidate, no d=50/d=100 launch, no HMC production readiness. |

## Source Facts

Author SIR row from
`third_party/audit/zhao_cui_tensor_ssm_p10/source/eg3_sir/mainscript.m`:

| Source line | Fact |
| --- | --- |
| `mainscript.m:13-16` | `d = 0`, `m = 18`, `n = m/2`, `T = 20`. |
| `mainscript.m:20-24` | Model is `ssmodel('sir_austria', d, m, n, T)` and then `setup`. |
| `mainscript.m:27-28` | Synthetic completion uses `rng(1)` before `complete(myModel)`. |
| `mainscript.m:42-45` | `N = 5e3`, `tau = 10`, `sqr = 1`; basis uses `ApproxBases(..., d + 2*m)`. |
| `mainscript.m:48-51` | Main option: `tt_method='random'`, `als_tol=1E-10`, `local_tol=1E-4`, `max_rank=40`, `max_als=8`, `init_rank=20`, `kick_rank=5`; low option has `max_als=2`. |
| `mainscript.m:53-55` | `rng(2)` then `full_sol(myModel, sqr, poly2, opt, lowopt, N, 4)` and `solve`. |

Full source route from
`third_party/audit/zhao_cui_tensor_ssm_p10/source/models/full_sol.m`:

| Source line | Fact |
| --- | --- |
| `full_sol.m:12-17` | `sol.samples`, `sol.L`, and `sol.mu` are dimension `model.d + 2*model.m`. |
| `full_sol.m:21-25` | `solve` initializes `[theta, x_0]`, pushes samples, then augments with previous state.  Generic order is `[theta, x_t, x_{t-1}]`; because author SIR has `d=0`, realized target is `[x_t, x_{t-1}]` with dimension `36`. |
| `full_sol.m:32-39` | Retained samples are generated by inverse SIRT, transformed by `L*r + mu`, and correction weights are `exp(-fun_post(r)) ./ eval_pdf(sirt, r)`. |
| `full_sol.m:49-61` | If step ESS is below `N/10` and `t != 1`, the author code enhances/recenters with doubled samples up to `ndouble > 4`. |
| `full_sol.m:64-69` | `computeL` gives `mu_temp`, `L_temp`; author samples weighted data and scales `L_temp` by `epd`. |
| `full_sol.m:72-80` | For `t > 1`, previous retained SIRT is marginalized and evaluated through the previous `L`/`mu` prefix. |
| `full_sol.m:91-96` | Random probes determine `const = min(fun_const(z))` before SIRT construction. |
| `full_sol.m:105-124` | With `sqr = 1`, the author constructs `TTSIRT`, using `sol.lowopt` for `t > 1`. |
| `full_sol.m:127` | Log marginal likelihood update is `log(sirt.z) - const`. |
| `full_sol.m:134-136` | Target log density is previous/prior density minus transition and likelihood terms, minus `const`. |

Support routines:

| Source | Fact |
| --- | --- |
| `models/computeL.m:11-19` | Weights are normalized; mean and covariance are weighted. |
| `models/computeL.m:20-32` | Cholesky regularization uses `1e-5 * eye(d)`; high-ESS case applies weighted quantile scaling. |
| `models/ESS.m:1-4` | ESS is `sum(w)^2 / sum(w.^2)` after removing NaN weights. |
| `deep-tensor.dev/src/Options/TTOption.m:39-46` | Defaults are `max_als=4`, `als_tol=1E-4`, `init_rank=20`, `kick_rank=2`, `max_rank=30`, `local_tol=1E-10`, `cdf_tol=1E-10`, `tt_method='amen'`. |
| `deep-tensor.dev/src/Options/TTOption.m:56-76` | Option parser accepts `random`, `amen`, and `fix_rank`; `kick_rank = 0` forces `fix_rank`. |

## Author Facts Versus Fixed-Variant Choices

Author facts that must be preserved:

- route: Zhao-Cui `full_sol`, not `pre_sol`, for this row;
- realized target: 36D `[x_t, x_{t-1}]`, with empty theta block because
  `d=0`;
- previous-marginal semantics for `t > 1`;
- proposal correction formula;
- squared route (`sqr = 1`) and TTSIRT branch;
- author rank envelope: `init_rank=20`, `max_rank=40`, `kick_rank=5`, with
  `max_als=8` on main fit and `max_als=2` on low fit;
- author sample count `N=5e3` and `epd=4`.

Fixed-HMC variant choices that must be labeled as such:

- deterministic/frozen draws and probe points;
- smaller smoke sample counts;
- bounded test domains and low-degree basis fixtures;
- rank-ladder subsets used to fit within memory/runtime;
- CPU-only diagnostics;
- tolerance thresholds for rank comparison;
- any reference bridge that is not the author's MATLAB/Octave route.

## Comparator Contract For P60-2

The P60-2 same-route comparator must compare two BayesFilter runs that differ
only by approximation strength:

| Field | Baseline row | Stronger comparator row |
| --- | --- | --- |
| Route | Zhao-Cui `full_sol` | Same |
| Target | Realized 36D `[x_t, x_{t-1}]` | Same |
| Observations | Same synthetic author-SIR observations | Same |
| Previous marginal | Same P59/P60 source-route semantics | Same |
| Correction formula | Same `exp(-fun_post(r)) / eval_pdf(sirt,r)` analogue | Same |
| Frozen diagnostics | Same probe points/draw identities when deterministic comparison is required | Same |
| Approximation strength | Fixed low row below | Fixed high row below |

The first planned P60-2 ladder is fixed here before seeing P60-2 results:

| Knob | `candidate_low` | `candidate_high` | Frozen? |
| --- | --- | --- | --- |
| Route | Zhao-Cui `full_sol` | Zhao-Cui `full_sol` | yes |
| Realized target | 36D `[x_t, x_{t-1}]` | 36D `[x_t, x_{t-1}]` | yes |
| Step count | `2` | `2` | yes |
| Retained sample count | `1` | `1` | yes |
| Fit sample count | `2` | `2` | yes |
| Basis family | same bounded Legendre fixture as P59-9e | same bounded Legendre fixture | yes |
| Fit degree | `0` | `1` | no: stronger approximation |
| TT/SIRT rank tuple | existing rank-1 P59/P60 row `(1, ..., 1)` | true rank-2 row `(1, 2, ..., 2, 1)`; if this row cannot normalize or otherwise fails, P60-2 must block rather than fall back to a degree-only comparator | no: stronger approximation |
| Frozen probe points | deterministic P60 probe set | same set | yes |
| Frozen retained evaluation points | retained points from both rows evaluated under both retained densities where APIs permit | same set | yes |

No additional comparator row may be substituted after observing P60-2 results.
If `candidate_high` cannot run within the runtime/memory preflight, P60-2 must
block rather than select a different stronger row post hoc.

If no strictly stronger same-route comparator can be run within budget, P60-2
must block with `BLOCK_P60_D18_SAME_ROUTE_RANK_CONVERGENCE`.

## Declared P60-2 Tolerances

These thresholds are fixed before seeing P60-2 comparator results.  They are
engineering rank-stability thresholds, not correctness thresholds.

| Diagnostic | Promotion threshold | Veto |
| --- | --- | --- |
| Log marginal likelihood delta per compared step | absolute delta `<= 5.0` for the smoke ladder; later production ladder must tighten before being called paper-scale | nonfinite value or threshold exceeded |
| Normalizer increment delta | absolute per-step delta `<= 5.0` for the smoke ladder | nonfinite increment or threshold exceeded |
| Correction log-weight range | both rows finite and range width finite | nonfinite or empty correction weights |
| ESS | finite and `0 < ESS <= sample_count` for every retained step | ESS invalid, zero, NaN, or exceeds sample count |
| Probe log-density delta | median absolute delta `<= 10.0` on frozen probes for smoke ladder | nonfinite probe values or threshold exceeded |
| Retained log-density delta | median absolute cross-evaluated delta `<= 10.0` on frozen retained evaluation points, when retained-density APIs expose comparable normalized densities; otherwise P60-2 must record `retained_density_delta_unavailable` and block rank-convergence promotion | nonfinite retained-density values, unavailable retained-density delta, or threshold exceeded |
| Replay/source invariants | route, target dimension, previous marginal, and correction semantics identical | any source-route drift |
| Memory/runtime | within 32 GiB practical memory cap and 20 minute visible-runtime cap for the focused smoke comparator | forecast breach, timeout, or runaway runtime |

If any threshold is changed after seeing P60-2 results, P60-2 must block even if
the changed threshold would pass.

If any non-approximation knob changes between `candidate_low` and
`candidate_high`, P60-2 must block rather than claim same-route rank
convergence.

## P60-3 Reference/Bridge Contract Boundary

P60-3 must choose one reference or bridge path before execution.  It may not use
the P60-2 same-route comparator as correctness evidence.  It may not use UKF,
memory, finite values, or wall time as correctness evidence.

Acceptable reference classes are:

1. trusted author-code route output with documented quantity mapping;
2. lower-rung dense reference bridge with explicit reduction map and same
   operation audit;
3. independently reviewed Monte Carlo bridge with uncertainty intervals.

Any tolerance, acceptance band, or uncertainty threshold widening after seeing
reference results forces `BLOCK_P60_D18_CORRECTNESS_BRIDGE`.

## Planned Artifacts

| Phase | Artifact |
| --- | --- |
| P60-1 result | `docs/plans/bayesfilter-highdim-zhao-cui-p60-1-source-rank-knobs-and-comparator-contract-result-2026-06-12.md` |
| P60-2 result | `docs/plans/bayesfilter-highdim-zhao-cui-p60-2-same-route-higher-rank-comparator-result-2026-06-12.md` |
| P60-3 result | `docs/plans/bayesfilter-highdim-zhao-cui-p60-3-same-target-reference-bridge-result-2026-06-12.md` |
| P60-4 result | `docs/plans/bayesfilter-highdim-zhao-cui-p60-4-validation-ladder-promotion-integration-result-2026-06-12.md` |
| Execution ledger | `docs/plans/bayesfilter-highdim-zhao-cui-p60-visible-execution-ledger-2026-06-12.md` |

## Decision Table

| Decision | Primary criterion | Veto diagnostics | Main uncertainty | Next justified action | Not concluded |
| --- | --- | --- | --- | --- | --- |
| Pass P60-1 comparator contract. | Met: source facts, fixed-variant choices, comparator contract, tolerances, vetoes, and artifacts are fixed before P60-2. | No UKF/memory/finite-value correctness proxy, no 18D target, no nonempty theta block, no old route, and no post-hoc tolerance loophole. | The smoke tolerances are engineering stability thresholds and may not be enough for a later paper-scale claim. | Run P60-2 same-route higher-rank comparator under this contract. | No rank convergence, correctness candidate, d=50/d=100 launch, or HMC readiness. |

## Token

`PASS_P60_1_SOURCE_RANK_COMPARATOR_CONTRACT`
