# P49-M0 Subplan: Route-Claim Governance

metadata_date: 2026-06-09
phase: P49-M0
status: PLAN_REVIEW_CONVERGED

## Objective

Repair R1 and R8 by making every Zhao--Cui claim route-specific:
`source_understanding`, `source_faithful_filtering`,
`gradient_bearing_adaptation`, `diagnostic_smoke`, or `blocked`.

## Evidence Contract

| Field | Contract |
| --- | --- |
| Question | Are current and future artifacts prevented from promoting fixed-branch or ad hoc gradient routes as source-faithful Zhao--Cui? |
| Baseline/comparator | P48 result and discrepancy ledger; P10/P34 source-audit non-claims; P46/P47 bounded fixed-grid artifacts; active P49 master/runbook/subplans. |
| Primary pass criterion | A governance matrix and claim-language patch list exist; route-specific pass tokens are defined; forbidden claim patterns are searchable. |
| Veto diagnostics | Any artifact says or implies current fixed branch is source-faithful adaptive Zhao--Cui without proof. |
| Not concluded | No code algorithm is repaired in M0. |

## Planned Work

1. Search P30--P49 artifacts and highdim tests for overbroad Zhao--Cui claims.
2. Create a route-claim matrix under `docs/plans`.
3. Patch documentation/test labels only when clearly wrong and scoped.
4. Define route-specific pass-token language for P49+ phases.
5. Verify the active P49 master program, subplans, and runbook use consistent
   result paths, pass/block tokens, and route labels.

## Repair Loop

If the search finds ambiguous wording, patch it if local and clearly in scope;
otherwise record it as a follow-up row.  If Claude says a claim is still
overbroad, patch and resubmit up to five rounds.

## Required Result

`docs/plans/bayesfilter-highdim-zhao-cui-p49-m0-route-claim-governance-result-2026-06-09.md`
